# OMS React 项目概览

## 🎯 项目简介

这是一个基于 Next.js 14 和 TypeScript 构建的现代化订单管理系统（OMS），完全遵循 Item 品牌的 UI 设计规范。项目采用最新的 React 技术栈，提供了完整的后台管理界面框架。

## 📊 项目架构

### UI架构特点

参考图片中的设计，项目采用了经典的后台管理系统布局：

```
┌─────────────────────────────────────────────────┐
│  Logo + Brand        [Search]      [Icons]      │  ← Header
├──────────┬──────────────────────────────────────┤
│          │                                       │
│  侧边栏   │           主内容区域                   │
│          │                                       │
│  导航菜单  │      卡片列表 / 数据展示               │
│          │                                       │
│  - Dashboard                                    │
│  - Orders                                       │
│  - Returns                                      │
│  - ...                                          │
│          │                                       │
│  [用户信息]│                                      │
└──────────┴──────────────────────────────────────┘
```

### 技术架构

```
┌─────────────────────────────────────────┐
│           Next.js 14 App Router         │
├─────────────────────────────────────────┤
│  React 18 + TypeScript                  │
├─────────────────────────────────────────┤
│  Tailwind CSS + shadcn/ui               │
├─────────────────────────────────────────┤
│  Radix UI (无障碍组件)                   │
└─────────────────────────────────────────┘
```

## 🎨 设计系统实现

### 颜色系统

根据 UI 规范，项目实现了完整的颜色系统：

**主色调**
- Primary Purple: `#763abf` - 用于主要操作按钮、活动状态
- Secondary Orange: `#F97316` - 用于强调和 CTA

**中性色**
- Background Light: `#fafafa`
- Background Dark: `#000000`
- Foreground Light: `#181818`
- Foreground Dark: `#ffffff`

**灰度系统**
- Mist: `#f2f2f2`
- Dove: `#e6e6e6`
- Steel: `#cccccc`
- Glacier: `#999999`
- Slate: `#808080`
- Iron: `#666666`
- Shadow: `#4d4d4d`

### 排版系统

**字体**: Satoshi (可变字体)

**字重**:
- Light (300) - 装饰性文本
- Regular (400) - 正文
- Medium (500) - 副标题
- Bold (700) - 标题
- Black (900) - 大标题

**字号层级**:
- 9xl (128px) - Hero 标题
- 8xl (96px) - 主标题
- 7xl (64px) - 次标题
- ...
- sm (14px) - 按钮文本
- xs (12px) - 标签

## 📁 项目结构详解

```
OMS React/
│
├── app/                              # Next.js App Router
│   ├── layout.tsx                   # 根布局
│   ├── page.tsx                     # 首页（重定向到 dashboard）
│   ├── globals.css                  # 全局样式（shadcn/ui 颜色变量）
│   │
│   ├── dashboard/                   # 仪表盘模块
│   │   └── page.tsx                # 统计卡片、最近订单、快捷操作
│   │
│   ├── orders/                      # 订单管理模块
│   │   └── page.tsx                # 订单列表、状态管理
│   │
│   ├── returns/                     # 退货管理模块
│   ├── purchase/                    # 采购管理模块
│   ├── logistics/                   # 物流跟踪模块
│   ├── inventory/                   # 库存管理模块
│   ├── product/                     # 产品目录模块
│   ├── events/                      # 事件日志模块
│   ├── integrations/                # 第三方集成模块
│   ├── pom/                         # 采购订单管理模块
│   ├── automation/                  # 自动化工作流模块
│   ├── customer-management/         # 客户管理模块
│   └── merchant-management/         # 商户管理模块
│
├── components/
│   ├── layout/                      # 布局组件
│   │   ├── sidebar.tsx             # 侧边栏导航（13个菜单项）
│   │   ├── header.tsx              # 顶部栏（搜索、通知、设置）
│   │   └── main-layout.tsx         # 主布局容器
│   │
│   └── ui/                          # shadcn/ui 组件
│       ├── button.tsx              # 按钮组件
│       ├── card.tsx                # 卡片组件
│       ├── input.tsx               # 输入框组件
│       ├── scroll-area.tsx         # 滚动区域组件
│       └── separator.tsx           # 分隔线组件
│
├── lib/
│   └── utils.ts                     # 工具函数（cn 类名合并）
│
├── public/                          # 静态资源
│
├── tailwind.config.ts               # Tailwind 配置（颜色、字体）
├── tsconfig.json                    # TypeScript 配置
├── package.json                     # 依赖管理
├── README.md                        # 项目说明
├── SETUP.md                         # 快速启动指南
└── PROJECT_OVERVIEW.md              # 本文件
```

## 🚀 核心功能

### 1. 侧边栏导航 (Sidebar)

**位置**: `components/layout/sidebar.tsx`

**功能**:
- 13个主导航菜单项
- 图标 + 文字标签
- 活动状态高亮（紫色背景）
- 悬停效果
- 底部用户信息卡片
- 响应式滚动

**菜单项**:
1. Dashboard (仪表盘图标)
2. Orders (购物车图标)
3. Returns (旋转图标)
4. Purchase (购物袋图标)
5. Logistics (卡车图标)
6. Inventory (包裹图标)
7. Product (盒子图标)
8. Events (日历图标)
9. Integrations (插头图标)
10. POM (文件图标)
11. Automation (闪电图标)
12. Customer Management (用户图标)
13. Merchant Management (商店图标)

### 2. 顶部栏 (Header)

**位置**: `components/layout/header.tsx`

**功能**:
- 全局搜索框
- 通知图标按钮
- 设置图标按钮
- 固定在顶部

### 3. 主布局 (MainLayout)

**位置**: `components/layout/main-layout.tsx`

**功能**:
- 组合侧边栏和顶部栏
- 提供内容区域容器
- 处理滚动和溢出

### 4. Dashboard 页面

**位置**: `app/dashboard/page.tsx`

**功能**:
- 4个统计卡片（订单、产品、客户、收入）
- 最近订单列表
- 快捷操作按钮
- 响应式网格布局

### 5. Orders 页面

**位置**: `app/orders/page.tsx`

**功能**:
- 订单列表展示
- 状态标签（待处理、处理中、已发货、已送达）
- 新建订单按钮
- 订单详情卡片

## 🎨 UI 组件库

### shadcn/ui 组件

项目使用 shadcn/ui 作为基础组件库，所有组件都遵循 Item 的设计规范：

**已实现的组件**:
- `Button` - 多种变体（default, outline, ghost, link）
- `Card` - 卡片容器及其子组件
- `Input` - 输入框
- `ScrollArea` - 滚动区域
- `Separator` - 分隔线

**组件特点**:
- 完全可定制
- 支持暗色模式
- 无障碍访问
- TypeScript 类型支持

## 🌈 主题系统

### CSS 变量

项目使用 CSS 变量实现主题系统，定义在 `app/globals.css`:

```css
:root {
  --primary: 267 53% 49%;        /* Purple */
  --secondary: 0 0% 86%;         /* Light Gray */
  --background: 0 0% 98%;        /* Almost White */
  --foreground: 0 0% 9%;         /* Almost Black */
  /* ... 更多变量 */
}

.dark {
  --primary: 267 53% 49%;        /* Purple (不变) */
  --background: 0 0% 0%;         /* Black */
  --foreground: 0 0% 100%;       /* White */
  /* ... 暗色模式变量 */
}
```

### Tailwind 配置

在 `tailwind.config.ts` 中扩展了颜色系统：

```typescript
colors: {
  primary: {
    DEFAULT: "hsl(var(--primary))",
    foreground: "hsl(var(--primary-foreground))",
  },
  sidebar: {
    DEFAULT: "hsl(var(--sidebar-background))",
    foreground: "hsl(var(--sidebar-foreground))",
    // ... 更多侧边栏颜色
  },
}
```

## 📱 响应式设计

### 断点

```typescript
screens: {
  "2xl": "1400px",
}
```

### 布局适配

- **桌面**: 侧边栏 + 主内容区
- **平板**: 可折叠侧边栏
- **移动**: 抽屉式侧边栏（待实现）

## 🔧 开发工具

### TypeScript 配置

- 严格模式启用
- 路径别名: `@/*` 指向根目录
- Next.js 插件支持

### ESLint 配置

- Next.js 核心规则
- 代码质量检查

### Git 配置

- 仓库: https://github.com/JayneKuo/OMS-React
- 分支: main
- 已同步到远程

## 📈 下一步开发建议

### 短期目标 (1-2周)

1. **完善 Dashboard**
   - 添加真实数据图表
   - 实现数据刷新
   - 添加日期范围选择器

2. **订单管理**
   - 订单详情页面
   - 订单创建表单
   - 订单状态更新
   - 订单搜索和筛选

3. **产品管理**
   - 产品列表
   - 产品详情
   - 产品编辑
   - 图片上传

### 中期目标 (1-2月)

4. **库存管理**
   - 库存跟踪
   - 库存预警
   - 库存调整

5. **客户管理**
   - 客户列表
   - 客户详情
   - 客户订单历史

6. **API 集成**
   - 后端 API 连接
   - 数据获取和缓存
   - 错误处理

### 长期目标 (3-6月)

7. **用户认证**
   - 登录/注册
   - 权限管理
   - 角色系统

8. **数据可视化**
   - 销售图表
   - 库存图表
   - 趋势分析

9. **高级功能**
   - 批量操作
   - 导入/导出
   - 报表生成
   - 自动化工作流

## 🛠️ 技术债务和优化

### 待优化项

1. **性能优化**
   - 实现虚拟滚动
   - 图片懒加载
   - 代码分割

2. **用户体验**
   - 加载状态
   - 错误提示
   - 空状态设计

3. **可访问性**
   - 键盘导航
   - 屏幕阅读器支持
   - ARIA 标签

4. **测试**
   - 单元测试
   - 集成测试
   - E2E 测试

## 📚 参考资源

- [Next.js 文档](https://nextjs.org/docs)
- [shadcn/ui 文档](https://ui.shadcn.com)
- [Tailwind CSS 文档](https://tailwindcss.com/docs)
- [Radix UI 文档](https://www.radix-ui.com)
- [Lucide Icons](https://lucide.dev)

## 👥 团队协作

### Git 工作流

1. 从 main 分支创建功能分支
2. 开发并提交代码
3. 创建 Pull Request
4. 代码审查
5. 合并到 main

### 命名规范

- **分支**: `feature/功能名`, `fix/问题描述`
- **提交**: 使用清晰的提交信息
- **组件**: PascalCase
- **文件**: kebab-case

## 📞 联系方式

- **仓库**: https://github.com/JayneKuo/OMS-React
- **问题反馈**: GitHub Issues

---

**最后更新**: 2024-12-08
**版本**: 0.1.0
**状态**: 开发中 🚧

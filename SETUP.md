# 快速启动指南

## 安装步骤

### 1. 安装依赖

```bash
npm install
```

### 2. 启动开发服务器

```bash
npm run dev
```

### 3. 访问应用

打开浏览器访问: [http://localhost:3000](http://localhost:3000)

## 项目特点

### ✅ 已完成的功能

- **完整的导航系统** - 13个主要模块的侧边栏导航
- **响应式布局** - 适配桌面和移动设备
- **Item品牌设计系统** - 遵循UI规范的颜色和排版
- **shadcn/ui组件** - 现代化的UI组件库
- **TypeScript支持** - 完整的类型安全
- **暗色模式支持** - 内置明暗主题切换

### 📦 主要模块

1. **Dashboard** - 仪表盘概览
2. **Orders** - 订单管理
3. **Returns** - 退货处理
4. **Purchase** - 采购管理
5. **Logistics** - 物流跟踪
6. **Inventory** - 库存管理
7. **Product** - 产品目录
8. **Events** - 事件日志
9. **Integrations** - 第三方集成
10. **POM** - 采购订单管理
11. **Automation** - 自动化工作流
12. **Customer Management** - 客户管理
13. **Merchant Management** - 商户管理

## 技术栈

- **Next.js 14** - React框架（App Router）
- **TypeScript** - 类型安全
- **Tailwind CSS** - 样式框架
- **Radix UI** - 无障碍UI组件
- **Lucide React** - 图标库

## 设计规范

### 颜色系统

- **主色**: `#763abf` (紫色) - 品牌主色
- **辅助色**: `#F97316` (橙色) - 强调色
- **背景色**: 
  - 浅色模式: `#fafafa`
  - 深色模式: `#000000`

### 字体

- **字体家族**: Satoshi (可变字体)
- **字重**: 300 (Light), 400 (Regular), 500 (Medium), 700 (Bold), 900 (Black)

## 开发建议

### 添加新页面

1. 在 `app/` 目录下创建新文件夹
2. 添加 `page.tsx` 文件
3. 使用 `MainLayout` 组件包裹内容
4. 在 `components/layout/sidebar.tsx` 中添加导航项

### 添加新组件

1. 在 `components/ui/` 目录下创建组件文件
2. 使用 Radix UI 作为基础
3. 应用 Tailwind CSS 样式
4. 遵循 shadcn/ui 的设计模式

### 样式规范

- 使用 Tailwind CSS 工具类
- 遵循 shadcn/ui 的颜色变量
- 保持组件的可复用性
- 确保响应式设计

## 常用命令

```bash
# 开发模式
npm run dev

# 构建生产版本
npm run build

# 启动生产服务器
npm start

# 代码检查
npm run lint
```

## 项目结构

```
OMS React/
├── app/                    # 页面路由
│   ├── dashboard/         # 仪表盘
│   ├── orders/            # 订单
│   └── ...                # 其他模块
├── components/
│   ├── layout/            # 布局组件
│   │   ├── sidebar.tsx   # 侧边栏
│   │   ├── header.tsx    # 顶部栏
│   │   └── main-layout.tsx
│   └── ui/                # UI组件
├── lib/
│   └── utils.ts           # 工具函数
└── public/                # 静态资源
```

## 下一步开发

### 建议的开发顺序

1. **完善Dashboard** - 添加真实数据和图表
2. **订单管理** - 实现完整的CRUD功能
3. **产品管理** - 构建产品目录系统
4. **库存管理** - 实现库存跟踪
5. **集成API** - 连接后端服务
6. **用户认证** - 添加登录和权限系统
7. **数据可视化** - 添加图表和报表

## 需要帮助？

- 查看 [Next.js 文档](https://nextjs.org/docs)
- 查看 [shadcn/ui 文档](https://ui.shadcn.com)
- 查看 [Tailwind CSS 文档](https://tailwindcss.com/docs)

## Git仓库

项目已同步到: [https://github.com/JayneKuo/OMS-React](https://github.com/JayneKuo/OMS-React)
